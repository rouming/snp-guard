[workspace]
members = [
    "src/common",
    "src/client",
    "src/server",
    "entity",
    "migration"
]
resolver = "2"

[profile.release]
strip = true
opt-level = "z"
lto = true
codegen-units = 1
panic = "abort"
